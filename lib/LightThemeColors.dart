import 'package:flutter/material.dart';

class LightThemeColors {
  static const Color primary = Colors.blue;        // اللون الأساسي (أزرق)
  static const Color background = Colors.white;    // لون الخلفية (أبيض)
  static const Color text = Colors.black;          // لون الخط (أسود)
}